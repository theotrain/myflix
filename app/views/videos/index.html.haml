- @categories.each do |cat|
  - if !cat.videos.empty?
    %article.video_category
      %header
        %h3= link_to cat.name
      .videos.row
        - cat.recent_videos.each do |video|
          .video.col-sm-2
            = link_to video, class:"vid_id_#{video.id}" do
              %img{:src => "/tmp/#{video.small_cover_url}"}