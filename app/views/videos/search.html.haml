%article.video_category
  %header
    %h3 Search Results
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to "", nil do
          %img(src="/tmp/#{video.small_cover_url}")